# Installation

`export NIX_CONFIG="experimental-features = nix-command flakes"`
`nix shell nixpkgs#home-manager`
`make`

# Tips

## Configure terminal

- Run `dconf watch /` and change your terminal configs with the GUI to see the config changes in real time
